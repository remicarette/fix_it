<div id="card-info">
  <div class="infos">

    <!-- AVATAR -->
    <div class="">
      <%= image_tag @booking.pro.photo, alt: "alttext", class: "avatar-pro-dashboard" %>
    </div>

    <!-- PRO FULL NAME -->
    <div class="title text-center">
      <%= @booking.pro.full_name %>
    </div>

    <!-- INFOS -->
    <div class="card-info-item info text-center">
      Vos infos
    </div>
    <div class="green-bar"></div>

    <!-- BRAND -->
    <%= render "bookings/card_info_item", info: "#{@booking.equipement.brand}" %>

    <!-- MODEL -->
    <%= render "bookings/card_info_item", info: "#{@booking.equipement.model}" %>

    <!-- BOOKING -->
    <div class="card-info-item rdv text-center">
      Rendez vous le <%= @booking.begin.strftime("%d/%m/%Y à %R") %> <%# @booking.begin, format: '%d/%m/%Y %k:%M' %>
    </div>
  </div>
</div>
