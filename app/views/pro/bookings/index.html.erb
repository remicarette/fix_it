<div class="container">
  <div class="row">
    <% @bookings.each do |booking| %>
      <div class="col-4">
        <div class="card">
          <div class="card-body">
            <h5><%= booking.client %></h5>
            <p><%= booking.begin.strftime("%d/%m/%Y à %R") %></p>
            <p><%= "#{booking.equipement.brand} #{booking.equipement.model}" %></p>
            <p><%= booking.begin.strftime("%d/%m/%Y à %R") %></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
