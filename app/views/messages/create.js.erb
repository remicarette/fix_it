function refreshForm(innerHTML) {
  const newReviewForm = document.getElementById('new_message');
  newReviewForm.innerHTML = innerHTML;
}

function addMessage(messageHTML) {
  const messages = document.getElementById('messages');
  messages.insertAdjacentHTML('beforeend', messageHTML);
}

console.log("create JS")

<% if @message.errors.any? %>
  refreshForm('<%= j render "bookings/form_chat", booking: @booking, message: @message %>');
<% else %>
  AddMessage('<%= j render "bookings/show", message: @message %>');
  refreshForm('<%= j render "bookings/form_chat", booking: @booking, message: Message.new %>');
<% end %>
