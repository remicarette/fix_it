<div class="container">
  <div class="row">
    <div class="col-12">
      <h1>Nos meilleures entreprises à <%= @results_geo.city.nil? ? @results_geo.city_district : @results_geo.city %></h1>
    </div>
      <%= render"shared/card_pro", array: @users.sort_by{|user| [user.first_free_slot, -user.average_stars_pro, -user.reviews.count]} %>
  </div>
</div>

